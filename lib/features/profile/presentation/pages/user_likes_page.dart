import 'package:flutter/material.dart';

class UserLikesPagePage extends StatelessWidget {
  const UserLikesPagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
