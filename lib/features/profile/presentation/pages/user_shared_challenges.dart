import 'package:flutter/material.dart';

class UserSharedChallengesPage extends StatelessWidget {
  const UserSharedChallengesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
